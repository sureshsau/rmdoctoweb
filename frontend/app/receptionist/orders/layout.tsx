"use client";

import ReceptionistLayout from "../layout";

export default function ReceptionistOrderLayout({ children }: { children: React.ReactNode }) {
    return <ReceptionistLayout>{children}</ReceptionistLayout>;
}
